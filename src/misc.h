#include <cglm/cglm.h>

/// @brief Indent by given number of double spaces
/// @param indent Number of double spaces
void misc_ind(size_t indent);

/// @brief Return whether the input is NULL, and print NULL if it is NULL
/// @param z Value to check
/// @return Whether the input is NULL
bool misc_not_null(void* z);
